# ---- Jelajah Halal :: nginx reverse proxy (production, real Let's Encrypt cert) ----
# Runs standalone (own container, own config, own cert) behind a thin SNI-passthrough
# edge router — this file never shares a process or config with any other site.
limit_req_zone $binary_remote_addr zone=api:10m rate=10r/m;

server {
    listen 443 ssl;
    http2 on;
    server_name halalmuslimtravel.com www.halalmuslimtravel.com;

    ssl_certificate     /etc/letsencrypt/live/halalmuslimtravel.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/halalmuslimtravel.com/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Permissions-Policy "geolocation=(self), microphone=(), camera=()" always;
    add_header Content-Security-Policy "default-src 'self'; img-src 'self' https://*.tile.openstreetmap.org data:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://unpkg.com; script-src 'self' https://unpkg.com; font-src 'self' https://fonts.gstatic.com; connect-src 'self'" always;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    client_max_body_size 2m;

    location /api/ {
        limit_req zone=api burst=20 nodelay;
        proxy_pass         http://web:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
    location / {
        proxy_pass         http://web:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}
